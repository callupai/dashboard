import { TokenGetter } from './interface';
import AppConfig from "./appconfig";
export declare class StatsManager {
    tokenGetter: TokenGetter;
    appConfig: AppConfig;
    constructor(appConfig: any, tokenGetter: TokenGetter);
    private static queryToParams(statsRequest);
    getStats(tableName: any, statsRequest: any): Promise<{}>;
}
export default StatsManager;
